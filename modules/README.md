# Terraform Modules

This folder contains reusable Terraform modules for the infrastructure.

The same modules will be called from the dev, staging, and prod environment folders with different environment-specific values.

Planned modules include networking, security, load balancing, compute, database, shared storage, and monitoring.